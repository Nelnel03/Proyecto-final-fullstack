import { describe, it, expect } from 'vitest';

describe('Configuración inicial de Testing', () => {
  it('Debe pasar esta prueba básica si Vitest está bien configurado', () => {
    expect(true).toBe(true);
  });
});
