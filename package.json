{
  "name": "proyecto-final-fullstack",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"BACK,FRONT\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/ReacProyecto\"",
    "setup": "npm run setup --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
